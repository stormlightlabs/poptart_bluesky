name: bsky_moderation
description: High-level Bluesky content moderation helpers ported for Poptart lex types.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.1 <4.0.0"

dependencies:
  characters: ^1.4.0
  poptart_core: ^0.1.0
  poptart_lex: ^0.1.0
  freezed_annotation: ^3.1.0

dev_dependencies:
  bluesky_text:
    path: ../bluesky_text
  lints: ^6.0.0
  test: ^1.26.2
